<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>
    <nav class="navbar navbar-light nav-height nav-block nav-container navbar-fixed-top">
      <div class="nav-display">
        <a class="nav-link" href="/">Home</a>
        <!--Only show dashboard and log out when user logs in ortherwise show log in option-->
        {{#if logged_in}}
        <a class="nav-link" href="/dashboard">Dashboard</a>
        <a id="logout" class="nav-link" href="/login">Logout</a>
        {{else}}
        <a class="nav-link" href="/login">login</a>
        <a class="nav-link" href="/signup">Signup</a>
        {{/if}}
      </div>
      <div class="title-container">
        <h1>The Tech Blog</h1>
      </div>
    </nav>
  </header>
  <main>
    {{{ body }}}
  </main>
  <footer>

  </footer>
  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>